spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/fabdata
    username: fab
    password: fab123
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
  data:
    redis:
      host: redis
      port: 6379
  cache:
    type: redis

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus,info
  metrics:
    export:
      prometheus:
        enabled: true
  tracing:
    sampling:
      probability: 1.0

otel:
  exporter:
    otlp:
      endpoint: http://jaeger:4317
  service:
    name: fab-data-backend

server:
  port: 8080
